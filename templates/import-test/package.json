{
  "name": "{{.Name}}",
  "version": "{{.Version}}",
  "description": "Import-time behavior test for {{.PackageName}}@{{.PackageVersion}}",
  "private": true,
  "type": "{{.ModuleType}}",
  "dependencies": {
    "{{.PackageName}}": "{{.PackageVersion}}"
  }
}
